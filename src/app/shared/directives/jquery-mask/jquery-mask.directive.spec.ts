import { JqueryMaskDirective } from './jquery-mask.directive';

describe('JqueryMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new JqueryMaskDirective();
    expect(directive).toBeTruthy();
  });
});

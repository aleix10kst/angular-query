import { BootstrapSelectDirective } from './bootstrap-select.directive';

describe('BootstrapSelectDirective', () => {
  it('should create an instance', () => {
    const directive = new BootstrapSelectDirective();
    expect(directive).toBeTruthy();
  });
});
